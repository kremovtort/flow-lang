{
  let x#1 = {
    let x#1 = {
      let x#1 = {
        let x#1 = x;
        let x#2 = fn(y#0) {
          let y#1 = 0;
          y#0 + y#1
        };
        x#1.map(x#2)
      };
      let x#2 = fn(y#0) {
        let y#1 = 0;
        y#0 > y#1
      };
      x#1.filter(x#2)
    };
    x#1.sum()
  };
  let x#2 = fn(y#0) {
    println(y#0)
  };
  let x#3 = printer_handle();
  x#1.for_each(x#2) with { let Printer = x#3 }
}
